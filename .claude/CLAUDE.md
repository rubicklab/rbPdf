# Rubick API — Claude Code

As regras e convenções deste projeto são **fonte única** em `.cursor/rules/`
(compartilhadas com o Cursor). Não duplique conteúdo aqui — edite os `.mdc`.

@../.cursor/rules/project.mdc
@../.cursor/rules/local.mdc
